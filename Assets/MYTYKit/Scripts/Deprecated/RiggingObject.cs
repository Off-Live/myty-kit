using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RiggingObject : MonoBehaviour
{
    // Start is called before the first frame update
    //public Processor processor;
    //public List<RiggingModel> rigModel;

    ////[SerializeField] private string _selectedModel;
    //private int _lastIndex = -1;

    //// Update is called once per frame
    //void Update()
    //{
    //    if (processor.CheckModel(_selectedModel))
    //    {
    //        LandmarkData landmark = processor.GetModel(_selectedModel);
    //        if (landmark.isDirty)
    //        {
    //            foreach (var model in rigModel)
    //            {
    //                model.CopyFromModel(landmark);
    //            }
    //        }
    //        landmark.isDirty = false;
    //    }
    //}

    //public int GetLastSelected() { return _lastIndex; }
    //public void SetLastSelected(int idx) { _lastIndex = idx; }


}
